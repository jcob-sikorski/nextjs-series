// Note: this page is not directly accessible 
//       until the page.tsx file is defined

// Note: only the content in the page.tsx file is returned to the client
//       so if we watn to use this component we have to import it 
//       in the page.tsx file

export default function LineChart() {
    return <h1>Line chart</h1>;
}