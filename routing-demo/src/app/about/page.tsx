// Note: static metadata configuration
export const metadata = {
    title: "About Codevolution", // Note: this title field replaces one defined in a metadata root layout
}

export default function About() {
    return <h1>About me</h1>;
}